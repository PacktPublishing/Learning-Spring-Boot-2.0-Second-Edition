server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gregturn/learning-spring-boot-config-repo

# tag::secured[]
security:
  user:
    password: password
# tag::secured[]
